## Add row counts from the log next to every key table
id: row-count-rule
type: sop
snippet: row-count-comment

Row counts from the SAS log should be added adjacent to the code that created that table. This is a team standard that allows validators to independently check their results. Not required for every intermediate table, but the more the better.
