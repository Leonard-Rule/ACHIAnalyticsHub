**Avatar** — circular headshot for experts/staff; falls back to initials on a brand color when no `src`.

```jsx
<Avatar name="Joseph W. Thompson" size="lg" />
<Avatar name="Craig Wilson" src="/experts/cwilson.jpg" />
```

Props: `size` (sm/md/lg), `color` (navy/teal/maroon).
