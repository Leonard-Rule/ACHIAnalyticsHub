**Checkbox / Radio** — selection control with teal selected fill. One component covers both via `type`.

```jsx
<Checkbox label="Subscribe to the weekly roundup" defaultChecked />
<Checkbox type="radio" name="topic" label="Maternal & infant health" />
```
